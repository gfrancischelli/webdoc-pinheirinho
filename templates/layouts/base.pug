include ../mixins/flash-messages

doctype html
html
    head
        meta(charset="utf-8")
        meta(name="viewport", content="initial-scale=1.0, user-scalable=no,maximum-scale=1,width=device-width")
        title= title || 'Webdoc'
        link(rel="shortcut icone", href="/favicon.ico", type="image/x-icon")
        link(href="/styles/main.css", rel="stylesheet")
     
        script(src='https://cdnjs.cloudflare.com/ajax/libs/riot/3.0.1/riot.min.js')
        script(src="https://use.fontawesome.com/f39734232b.js")
     
        block css
        block head

    body
        include ../includes/header
        #main 
            block intro
            +flash-messages(messages)
            block content
        #footer Footer
    
    script(src='/js/bundle.js')

    block js
